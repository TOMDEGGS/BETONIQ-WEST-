{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r zeropay_backend/requirements.txt"
  },
  "deploy": {
    "startCommand": "cd zeropay_backend && uvicorn main:app --host 0.0.0.0 --port $PORT",
    "restartPolicyType": "ON_FAILURE"
  }
}
